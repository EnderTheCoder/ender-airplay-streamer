@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/AirplayStreamerTargets.cmake")

check_required_components(AirplayStreamer)

find_package(OpenSSL 1.1.1 REQUIRED)
find_package(PkgConfig REQUIRED)
pkg_check_modules(FFMPEG REQUIRED IMPORTED_TARGET
		libavcodec
		libavformat
		libavutil
)

